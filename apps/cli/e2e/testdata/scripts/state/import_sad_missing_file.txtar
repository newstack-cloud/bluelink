# Test state import fails in headless mode without --file flag

! exec bluelink state import
stderr 'file'
stderr 'non-interactive'

-- bluelink.config.toml --
# Empty config file to prevent config loading error
