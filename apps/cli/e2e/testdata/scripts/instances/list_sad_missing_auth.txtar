# Test list fails with missing auth

wait_engine

! exec bluelink instances list --connect-protocol=tcp --engine-endpoint=$BLUELINK_ENGINE_ENDPOINT --engine-auth-config-file=nonexistent-auth.json
stderr 'no such file or directory'

-- bluelink.config.toml --
