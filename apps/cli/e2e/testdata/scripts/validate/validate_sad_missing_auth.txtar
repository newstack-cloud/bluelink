# Test validate fails when auth config file is missing

! exec bluelink validate --blueprint-file=project.blueprint.yaml --connect-protocol=tcp --engine-endpoint=http://localhost:8325
stderr 'engine.auth.json'

-- bluelink.config.toml --
# Empty config file

-- project.blueprint.yaml --
version: "2024-04-01"
resources:
  testResource:
    type: example/resource
    metadata:
      displayName: Test Resource
