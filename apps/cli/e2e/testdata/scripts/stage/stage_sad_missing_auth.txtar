# Test stage fails when auth config file does not exist

wait_engine

! exec bluelink stage --blueprint-file=s3://test-blueprints/valid.blueprint.yaml --connect-protocol=tcp --engine-endpoint=$BLUELINK_ENGINE_ENDPOINT --engine-auth-config-file=nonexistent-auth.json
stderr 'no such file or directory'

-- bluelink.config.toml --
# Empty config file
