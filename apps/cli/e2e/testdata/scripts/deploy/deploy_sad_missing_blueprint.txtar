# Test deploy fails with missing blueprint file

wait_engine

! exec bluelink deploy --blueprint-file=nonexistent.blueprint.yaml --instance-name=test-instance --stage --auto-approve --skip-prompts --connect-protocol=tcp --engine-endpoint=$BLUELINK_ENGINE_ENDPOINT --engine-auth-config-file=engine.auth.json
stdout 'could not be resolved'

-- bluelink.config.toml --

-- engine.auth.json --
{
  "method": "apiKey",
  "apiKey": "test-api-key"
}
