# Test deploy fails without instance identifier

wait_engine

! exec bluelink deploy --blueprint-file=s3://test-blueprints/lambda-function.blueprint.yaml --stage --auto-approve --skip-prompts --connect-protocol=tcp --engine-endpoint=$BLUELINK_ENGINE_ENDPOINT --engine-auth-config-file=engine.auth.json
stderr 'instance-name'

-- bluelink.config.toml --

-- engine.auth.json --
{
  "method": "apiKey",
  "apiKey": "test-api-key"
}
