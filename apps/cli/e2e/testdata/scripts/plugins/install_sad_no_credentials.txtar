# Test plugins install fails when no credentials are configured

# Don't save any credentials
env HOME=$WORK

# Set plugin installation directory to a temp location
env BLUELINK_DEPLOY_ENGINE_PLUGIN_PATH=$WORK/plugins

# Run install without credentials - should fail with clear error
! exec bluelink plugins install localhost:18080/bluelink/test-provider@1.0.0
stdout 'no credentials found'

-- bluelink.config.toml --
# Empty config file
