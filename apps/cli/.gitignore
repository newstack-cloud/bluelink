# Code generated pre-build, (e.g genny generics templates)
gen-*.go

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
coverage.txt
coverage.html
coverage

# Test report for sonar.
report.json

# Dependency directories (remove the comment below to include it)
# vendor/

# Any log files.
*.log

# Test shell scripts for manually testing the CLI
# in non-interactive mode.
test-*.sh

!.env.test

# Test blueprint files used when testing the CLI during development.
*.blueprint.yaml
# But allow e2e test fixtures
!e2e/testdata/fixtures/*.blueprint.yaml

.claude
CLAUDE.md

engine.auth.json

# E2E test provider binary (built by docker-compose)
e2e/testprovider/bluelink-test-provider

# Migration files that are copied from the blueprint state library
# for integration tests, primarily for the export and import functionality.
internal/stateio/postgres
